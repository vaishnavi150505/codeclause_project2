numbers = sorted([float(x) for x in input("Enter numbers separated by space: ").split()])
n = len(numbers)

if n == 0:
    print("No numbers entered")
elif n % 2 == 1:
    median = numbers[n // 2]
else:
    median = (numbers[n // 2 - 1] + numbers[n // 2]) / 2

print("The median is:", median)
